# AGENTS.md

## Cursor Cloud specific instructions

### Repository State

This repository (`tan-poker`) is freshly initialized and contains **no application code** — only a `README.md`. There are no dependency manifests, build scripts, configuration files, or source code directories.

### Development Environment

- No dependencies to install.
- No build, lint, or test commands available.
- No services to run.

When application code is added, update this section with relevant setup, build, lint, test, and run instructions.
